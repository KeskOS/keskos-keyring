KeskOS Keyring Placeholder

This package intentionally does not ship any real GPG keys yet.

It exists so the repo can add a proper signed keyring later without changing
package naming or installer logic.
